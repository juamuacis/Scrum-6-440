import React from 'react'
import ProfileInformation from '../Components/Items/ProfileInformation';

const Profile = () => {
    return (    
            
            <section>
                <ProfileInformation/>
            </section>
    );
};

export default Profile;